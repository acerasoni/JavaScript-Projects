class animal extends entity {

    constructor(x, y, texture) {

        super(x, y, texture);
        // Food
        this.food = 50;
        // Water
        this.water = 50;

        // Reproduction

        // Oxygen
        this.oxygen = 50;
    }
}